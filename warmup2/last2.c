#include <string.h>

/**
 * Given a string, return the count of the number of times that a substring 
 * length 2 appears in the string and also as the last 2 chars.
 * Example: 
 * last2("hixxhi") → 1
 * last2("xaxxaxaxx") → 1
 * last2("axxxaaxx") → 2
 */
int last2(const char* str) {
  // TODO: implement this function
  return 0;
}
