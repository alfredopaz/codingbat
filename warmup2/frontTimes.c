#include <string.h>
#include <stdlib.h>

/**
 * Given a string and a non-negative int n, return n copies of the "front" 
 * of the string. The "front" is the first 3 chars, or whatever is there 
 * if the string is less than length 3.
 * 0 <= n < 10
 * Example: frontTimes("Chocolate", 2) → "ChoCho"
 */
char* frontTimes(const char* str, int n) {
    // TODO: implement this function
    char* result = malloc(1);
    result[0] = '\0';
    return result;
}
