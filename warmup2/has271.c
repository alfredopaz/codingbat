#include <stdbool.h>

/**
 * Given an array of ints, return true if it contains a 2,7,1 pattern 
 * somewhere: a value, followed by the value plus 5, followed by the value minus 1.
 * Example: 
 * has271([1, 2, 7, 1], 4) → true
 * has271([1, 2, 8, 1], 4) → false
 * has271([2, 7, 1], 3) → true
 */
bool has271(const int* nums, int numsLen) {
  // TODO: implement this function
  return false;
}
