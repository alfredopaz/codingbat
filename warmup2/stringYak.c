#include <string.h>
#include <stdlib.h>

/**
 * Suppose the string "yak" is unlucky. Given a string, return a version 
 * where all the "yak" substrings are removed.
 * Example: stringYak("yakpak") → "pak"
 */
char* stringYak(const char* str) {
  // TODO: implement this function
  char* result = malloc(1);
  result[0] = '\0';
  return result;
}
