#include <stdbool.h>

/**
 * Given an array of ints, return true if one of the first 4 elements in the array is a 9.
 * Example: 
 * arrayFront9([1, 2, 9, 3, 4], 5) → true
 * arrayFront9([1, 2, 3, 4, 9], 5) → false
 * arrayFront9([1, 2, 3, 4, 5], 5) → false
 */
bool arrayFront9(const int* nums, int numsLen) {
  // TODO: implement this function
  return false;
}
