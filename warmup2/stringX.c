#include <string.h>
#include <stdlib.h>

/**
 * Given a string, return a version where all the "x" chars have been removed.
 * Example: stringX("xabxxxcd") → "abcd"
 */
char* stringX(const char* str) {
  // TODO: implement this function
  char* result = malloc(1);
  result[0] = '\0';
  return result;
}
