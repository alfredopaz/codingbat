#include <string.h>
#include <stdlib.h>

/**
 * Return a new string made of every other char starting with the first.
 * Example: 
 * stringBits("Hello") → "Hlo"
 * stringBits("Hi") → "H"
 * stringBits("Heeololeo") → "Hello"
* 
 */
char* stringBits(const char* str) {
  // TODO: implement this function
  char* result = malloc(1);
  result[0] = '\0';
  return result;
}
