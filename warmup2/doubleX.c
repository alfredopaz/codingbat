#include <stdbool.h>
#include <string.h>

/**
 * Return true if the first instance of "x" in the string is immediately 
 * followed by another "x".
 * Example: doubleX("axxbb") → true
 */
bool doubleX(const char* str) {
    // TODO: implement this function
    return false;
}
