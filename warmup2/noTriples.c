#include <stdbool.h>

/**
 * Given an array of ints, return true if the array does not contain 
 * any triples (three of the same value in a row).
 * Example: 
 * noTriples([1, 1, 2, 2, 1], 5) → true
 * noTriples([1, 1, 2, 2, 2, 1], 6) → false
 * noTriples([1, 1, 1, 2, 2, 2, 1], 7) → false
 */
bool noTriples(const int* nums, int numsLen) {
  // TODO: implement this function
  return false;
}
